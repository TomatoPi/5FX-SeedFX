#pragma once

#include "jack.hpp"

namespace sfx
{
  namespace jack
  {
    enum class value_type_t {
      Invalid,
      ErrorCode,
      String,
      Id,
      Size,
      Integer,
      Float,
      Object,
    };

    struct value_descriptor_t {

    };

    struct field_descriptor_t {

    };

    struct struct_descriptor_t {

    };

    struct api_descriptor_t {

    };
  }
  // namespace jack
}
// namespace sfx